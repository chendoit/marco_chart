# WN-2025-03-22 網頁載入效能優化 ETag 快取

## 起因

網頁載入 `/charts/{group}` 時因後端每次 API 請求都即時重算所有圖表資料（pandas 運算：pkl 讀取、DataFrame join、interpolate、date filter），導致等待時間長。以「每日天啟」為例，15 組 chart 每組 3~7 個 series，使用者必須等全部算完才能看到第一張圖。

## 分析

效能瓶頸拆解：
- **後端**：`generate_chart_data()` 每次 API 呼叫都重算，`_series_cache` 僅快取原始 pkl 讀取，不快取運算結果
- **前端**：一次 fetch 整個 group 所有 chart，全部回來後才渲染，使用者看到長時間白屏
- **資料量**：pkl 總量 21 MB / 198 檔，大檔可達 800 KB

選擇方案 B（HTTP ETag / 304 機制）：
- 標準 HTTP 快取，瀏覽器原生支援
- 後端用 pkl 檔修改時間產生版本號，自動偵測資料變更
- 不需要 watchdog 套件、不需要手動觸發清快取

## 修改內容

### 1. `flask_highcharts/app/utils/highcharts_utils.py`
- 新增 `clear_series_cache()` 函數，暴露清除 `_series_cache` 的能力
- `app/utils/__init__.py` 同步匯出

### 2. `flask_highcharts/main.py`（核心變更）
- `_compute_data_version()`：掃描 `data/` 下所有 pkl 的 `os.path.getmtime()`，取最大值做 MD5 hash 前 12 碼作為版本號
- `_refresh_data_version_if_needed()`：每次 API 請求用 `os.stat(data/)` 檢查目錄 mtime（微秒級），有變化才重新掃描 pkl mtime → 更新 `data_version` → 清除 `_api_cache` + `_series_cache`
- `_api_cache` dict：快取每個 group 的完整 JSON 回應字串
- `/api/chart/{group}`：
  - 回應帶 `ETag: "{data_version}"` header
  - 收到 `If-None-Match` 且版本相同 → 回 `304 Not Modified`（無 body）
  - 版本不同 → 檢查 `_api_cache` → 有就直接用，沒有就重算並存入快取
- `/api/update_groups`：手動熱更新時同步清除所有快取
- 抽出 `_build_group_charts()` 純運算函數，與 HTTP 邏輯分離

### 3. `flask_highcharts/templates/charts.html`
- 新增 loading spinner（CSS 動畫旋轉圓圈 + 「載入圖表中...」文字）
- fetch 完成或失敗後自動移除 loading indicator

## 驗證結果

- Flask 啟動成功，19 個 group 模組全部載入
- `data_version` 初始化為 `d93bad92b74d`（基於當前 pkl 修改時間）
- 快取流程：
  - 首次請求：正常計算 → 存入 `_api_cache` → 回 200 + ETag
  - 第二次起：記憶體快取命中 → 毫秒級回應
  - 瀏覽器重載：帶 `If-None-Match` → 版本相同 → 304 無 body
  - pkl 更新後：`os.stat(data/)` 偵測到 mtime 變化 → 自動清快取 → 下次請求重算
