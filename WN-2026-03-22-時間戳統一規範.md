# WN-2026-03-22-時間戳統一規範

## 起因

發現 VIX、SPX 等 MacroMicro series 的 pkl 資料中，datetime 時間部分為 `08:00:00`，而 ETF（ProShares CSV）、CBOE 等其他來源為 `00:00:00`。不同來源的時間戳不一致，在做 DataFrame join、reindex、日期比較時可能造成 off-by-one 或 join miss 的隱患。

## 分析

### 問題根源

各資料來源的 datetime 產生方式不同：

| 來源 | 腳本 | 解析方式 | 修改前時間 |
|------|------|----------|------------|
| MacroMicro series | `get_m_square_series.py` | `datetime.fromtimestamp(unix_ms/1000)` | `08:00:00` (UTC+8 本機) |
| MacroMicro series (1970前) | `get_m_square_series.py` | `epoch + timedelta(seconds=ts)` | `00:00:00` (UTC 直算) |
| CBOE 指數 | `get_cboe_index.py` | `strptime("%Y-%m-%d")` | `00:00:00` |
| ProShares ETF CSV | `get_etf_csv.py` | `strptime("%m/%d/%Y")` | `00:00:00` |
| MacroMicro ETF API | `get_m_square_etf.py` | `strptime("%Y-%m-%d")` | `00:00:00` |
| MacroMicro Chart API | `get_m_square_chart.py` | `strptime("%Y-%m-%d")` | `00:00:00` |
| CFTC 報告 | `get_ctfc_series.py` | `datetime.combine(date, min.time)` | `00:00:00` |
| FRED / NY Fed | `get_fed_series.py` | `strptime` / `to_pydatetime()` | `00:00:00` |

### 設計考量

MacroMicro 的 `datetime.fromtimestamp()` 在 UTC+8 環境下產生 `08:00:00` 是正確的行為——MacroMicro 是台灣網站，其 Unix 時間戳以 UTC 為基準，在台灣時區 (UTC+8) 轉換後會產生 08:00。此設計有其用意，不應修改。

因此決定**以 MacroMicro series 的 08:00:00 為標準**，將所有其他來源統一。

### 額外發現

`get_m_square_series.py` 中有兩條處理路徑：
- **正 timestamp** (1970後)：`fromtimestamp()` → `08:00:00` ✓
- **負 timestamp** (1970前)：`epoch + timedelta` → `00:00:00` ✗

這導致同一個 pkl（如 S&P500 從 1927 年開始）內部 1970 年前後的時間不一致。

## 修改內容

### 1. 程式碼修改（7 個腳本）

所有抓取腳本在日期解析後加上 `.replace(hour=8)`：

- **get_cboe_index.py** 第 38 行：`strptime(...).replace(hour=8)`
- **get_etf_csv.py** 第 96 行：`strptime(...).replace(hour=8)`
- **get_m_square_etf.py** 第 66 行：`strptime(...).replace(hour=8)`
- **get_m_square_chart.py** 第 53 行：`strptime(...).replace(hour=8)`
- **get_ctfc_series.py** 第 151 行：`datetime.combine(...).replace(hour=8)`
- **get_fed_series.py** 第 42 行：`to_pydatetime().replace(hour=8)`
- **get_fed_series.py** 第 108 行：`strptime(...).replace(hour=8)`
- **get_m_square_series.py** 第 71 行：負 timestamp 路徑加 `.replace(hour=8)`

### 2. 時間戳規範註解

在所有 7 個抓取腳本頂部加上統一註解：

```
# [時間戳規範] 所有寫入 pkl 的 datetime 時間部分統一為 08:00:00 (UTC+8)
# 以與 MacroMicro series 的 fromtimestamp() 產出一致。新增抓取腳本時請遵循此規範。
```

### 3. 現有 pkl 批量修正

使用一次性腳本掃描 `data/` 目錄下所有 pkl 檔案，將 datetime 時間為 `00:00:00` 的紀錄改為 `08:00:00`。

## 驗證結果

- 批量修正：119 個檔案，共修正 192,722 筆紀錄
- 驗證：194 個 pkl 檔案全部通過，所有 datetime 時間部分均為 `08:00:00`
- 一次性腳本已清理刪除

## 規範總結

**新增抓取腳本時，請確保所有寫入 pkl 的 datetime 時間部分為 `08:00:00`。**

方法：在日期解析後加上 `.replace(hour=8)`，例如：
```python
dt = datetime.strptime(date_str, "%Y-%m-%d").replace(hour=8)
```
