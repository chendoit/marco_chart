# WN-2026-04-01 CME FTP 資料總覽與取得方式

## 起因

探索 CME Group FTP（`https://www.cmegroup.com/ftp/`）完整目錄結構，盤點所有可用資料集，評估哪些對現有分析（外匯、利率、VIX、CFTC 持倉）有補充價值。

## 反爬蟲限制

所有 FTP 目錄的檔案下載均受反爬蟲保護：
- `curl.exe` → timeout
- `Invoke-WebRequest` → 403 JSON（IP blocked）
- **唯一成功方法**：Playwright 開啟真實瀏覽器 → 手動登入 CME 帳號 → `page.evaluate(fetch(..., {credentials: 'include'}))` 取得檔案內容

注意：**目錄列表頁面（HTML）可以直接 WebFetch/瀏覽器存取**，只有實際檔案下載被擋。

## FTP 目錄結構總覽

```
https://www.cmegroup.com/ftp/
├── irs/                    ★ 利率交換收盤報價（rolling ~6 天）
├── daily_volume/           ★ 每日成交量+未平倉量（2014~至今，完整歷史）
├── settle/TCF/             　交割轉換係數（2023/12~至今）
├── fprf/                   　期貨定價參考檔（近 3 個月）
├── delivery_reports/       　交割報告
├── cash_settled_commodity_index_prices/  　僅育肥牛指數（Feeder Cattle）
├── webmthly/               　月度交易統計報告（2013~至今，年度 ZIP）
├── span/                   　SPAN 保證金參數
├── pub/                    　上述目錄的鏡像
├── efrp/                   　（2023/06 停更）
├── fix/                    　（2022/07 停更）
├── grs/                    　（2022/07 停更）
├── SBEFix/                 　（2022/07 停更）
└── sdr/                    　（2022/07 停更）
```

## 有價值的資料集詳細說明

### 1. `irs/` — 利率交換收盤報價

- **檔案命名**：`irs_close_quotes_{PRODUCT}_{YYYYMMDD}.csv`
- **保留期**：最近 ~6 個交易日（rolling window），無長期歷史
- **更新時間**：每交易日約 20:00 ET

**可用產品（30 種）**：

| 類別 | 代碼 | 幣別/說明 |
|------|------|----------|
| OIS | OISUSD, OISAUD, OISCHF, OISCOP, OISGBP, OISINR, OISJPY, OISMXN, OISSGD, OISZAR | 各幣別隔夜指數掉期 |
| IRS | AUD3B, AUD6B, EUR1E, EUR3E, EUR6E | 澳幣 BBSW、歐元 EURIBOR |
| 新興市場 IRS | BRLCDI, IRSCLP, IRSCNY, IRSCZK, IRSDKK, IRSHKD, IRSHUF, IRSKRW, IRSNOK, IRSNZD, IRSPLN, IRSSEK, IRSZAR | 各新興市場 |
| Basis Swap | BSPUSDSOFR | USD SOFR Basis Swap |
| 其他 | CADOIS, MXN_PAA_Rate | 加幣 OIS、墨西哥 PAA |

**CSV 格式**：
```csv
CURVE_NAME,TENOR,RATE
USD FEDFUNDS 1D,1 Year,3.657723
```

**OISUSD 可用期限**：1Y, 2Y, 5Y, 10Y, 30Y（缺 1M, 3M, 6M）

### 2. `daily_volume/` — 每日成交量與未平倉量

- **檔案命名**：`daily_volume_{YYYYMMDD}.xlsx`（另有不帶日期的最新版 `daily_volume.xlsx`）
- **歷史深度**：2014-01-02 至今（3000+ 檔案），**完整保留不刪除**
- **檔案大小**：每個約 98~105 KB，全部約 300MB
- **涵蓋範圍**：CME 集團旗下四個交易所所有期貨與選擇權

**交易所**：XCME, XCBT (CBOT), XNYM (NYMEX), XCEC (COMEX)

**欄位**：

| 欄位 | 說明 |
|------|------|
| Trade Date | 交易日期 (YYYYMMDD) |
| Exchange Code | 交易所代碼 |
| Product Code / Description | 產品代碼與名稱 |
| Product Type | Future 或 Option |
| Put/Call, Strike Price | 選擇權資訊 |
| Contract Year & Month | 合約年月 |
| Open Interest | 未平倉量 |
| Total Volume | 總成交量 |
| Globex / Floor / PNT / Block Volume | 按交易平台拆分的成交量 |

**與 CFTC 的互補**：
- CFTC COT 報告是週頻（週二截止，週五公布），daily_volume 是日頻
- CFTC 有多空持倉分類（Commercial/Non-commercial），daily_volume 只有總量
- 日圓期貨 product code：Globex `6J`，Clearing `J1`
- 搭配使用：日頻 Volume+OI 變化方向 + CFTC 週報多空部位 → 更即時判斷資金流向

### 3. 其他（價值較低）

| 目錄 | 內容 | 備註 |
|------|------|------|
| `settle/TCF/` | 交割轉換係數 CSV | 2023/12 起，持續累積 |
| `fprf/` | 期貨定價參考檔 (XML zip) | 近 3 個月，含 CBOT/CME/COMEX/NYMEX |
| `webmthly/` | 月度統計報告 ZIP | 2013 起，每月一個 |
| `cash_settled_commodity_index_prices/` | 僅育肥牛 (Feeder Cattle) 指數 | 與外匯/利率無關 |

## 結論

- **最有潛力的資料集**：`daily_volume/`（完整歷史的日頻成交量+OI，可搭配 CFTC 做更細緻的分析）
- **irs/ 的 OIS 資料**：只保留 6 天且缺 3M 期限，不適合替代 MacroMicro
- **所有檔案下載都需要 Playwright 瀏覽器 session**（CME 帳號登入），無法全自動化
- 若要下載 daily_volume 全部歷史（3000+ xlsx），需考慮 rate limit 風險，建議加延遲並分批處理
