# WN-2026-04-03 CME 資料取得討論彙整

## 起因

專案中多次探索 CME Group 相關資料來源（FTP 檔案下載、CFTC COT 報告），用於補充 MacroMicro 延遲或缺失的數據。本文彙整所有 CME 相關討論、技術發現與實作成果。

## 一、CME FTP 目錄探索

### 1.1 目錄結構（2026-04-01 調查）

```
https://www.cmegroup.com/ftp/
├── irs/                    ★ 利率交換收盤報價（rolling ~6 天）
├── daily_volume/           ★ 每日成交量+未平倉量（2014~至今，完整歷史）
├── settle/TCF/             　交割轉換係數（2023/12~至今）
├── fprf/                   　期貨定價參考檔（近 3 個月）
├── delivery_reports/       　交割報告
├── webmthly/               　月度交易統計報告（2013~至今）
├── span/                   　SPAN 保證金參數
├── pub/                    　上述目錄的鏡像
├── efrp/                   　（2023/06 停更）
├── fix/                    　（2022/07 停更）
├── grs/                    　（2022/07 停更）
├── SBEFix/                 　（2022/07 停更）
└── sdr/                    　（2022/07 停更）
```

### 1.2 反爬蟲限制（重要）

所有 FTP 檔案下載均受反爬蟲保護：

| 方法 | 結果 |
|------|------|
| `curl.exe` | timeout |
| `Invoke-WebRequest` | 403 JSON（IP blocked） |
| Playwright `page.request.get` | HTTP 403 |
| Playwright `page.goto` 直接導航到 CSV | HTTP 400 |

**唯一成功方法**：Playwright `headless=False` → 手動登入 CME 帳號 → `page.evaluate(fetch(..., {credentials: 'include'}))` 取得檔案內容。

注意：**目錄列表頁面（HTML）可以直接存取**，只有實際檔案下載被擋。

### 1.3 Playwright 技術踩坑

1. 背景執行不支援 `input()` → 改用信號檔偵測機制
2. CME 首頁 `wait_for_load_state("networkidle")` timeout（持續載入廣告） → 直接刪除此等待
3. FTP 頁面可能被 SSO 重定向中斷 → 改用 `wait_until="domcontentloaded"` 加重試迴圈

## 二、CME FTP OIS 資料測試

### 2.1 背景

MacroMicro 的 OIS 數據（series 1150441, 1150443）只更新到 2026-03-20，導致「美國 OIS 利差 1Y-3M」圖表落後約 10 天。嘗試從 CME FTP `/irs/` 取得替代資料。

### 2.2 測試結果

成功下載 6 個 OISUSD CSV（3/24~3/31），格式：

```csv
CURVE_NAME,TENOR,RATE
USD FEDFUNDS 1D,1 Year,3.657723
USD FEDFUNDS 1D,2 Years,3.560841
USD FEDFUNDS 1D,5 Years,3.555482
USD FEDFUNDS 1D,10 Years,3.813434
USD FEDFUNDS 1D,30 Years,4.05584
```

| 項目 | 內容 |
|------|------|
| 可用期限 | 1Y, 2Y, 5Y, 10Y, 30Y |
| 缺少期限 | 1M, 3M, 6M（無短期限） |
| 保留期 | 最近 ~6 天（rolling window） |
| 可用產品 | 30 種（OISUSD/AUD/CHF/GBP/JPY 等 + IRS + Basis Swap） |

### 2.3 與 MacroMicro 數值比較（3/20）

| 來源 | OIS 1Y |
|------|--------|
| MacroMicro | 3.7386 |
| CME FTP (3/24) | 3.7640 |

### 2.4 結論

- **缺 3M 期限**，無法計算 1Y-3M 利差，不能完整替代 MacroMicro
- 只保留 6 天，不適合作為歷史資料來源
- 需 CME 帳號登入 + Playwright，無法全自動化
- MacroMicro OIS 延遲為資料來源端問題，建議等待自動追上

## 三、CME FTP Daily Volume（待開發）

### 3.1 資料特徵

| 項目 | 內容 |
|------|------|
| 路徑 | `https://www.cmegroup.com/ftp/daily_volume/` |
| 檔案格式 | `daily_volume_{YYYYMMDD}.xlsx` |
| 歷史深度 | 2014-01-02 至今（3000+ 檔案），完整保留不刪除 |
| 涵蓋範圍 | XCME, XCBT, XNYM, XCEC 四交易所所有期貨與選擇權 |
| 檔案大小 | 每個 ~100KB，全部約 300MB |

### 3.2 欄位

Trade Date, Exchange Code, Product Code/Description, Product Type (Future/Option), Put/Call, Strike Price, Contract Year/Month, Open Interest, Total Volume, Globex/Floor/PNT/Block Volume

### 3.3 與 CFTC 的互補

- CFTC COT 是**週頻**（週二截止→週五公布），daily_volume 是**日頻**
- CFTC 有多空持倉分類，daily_volume 只有總量
- 搭配使用：日頻 Volume+OI 變化方向 + CFTC 週報多空部位 → 更即時判斷資金流向

### 3.4 開發障礙

- 下載同樣受反爬蟲限制，需 Playwright + CME 帳號
- 3000+ xlsx 批量下載需考慮 rate limit，建議加延遲分批處理
- 目前尚未實作

## 四、CFTC/CME 持倉資料（已實作）

### 4.1 Legacy COT 報告（初始實作）

透過 `get_ctfc_series.py` 使用 `pycot-reports` 套件，從 CFTC 公開 API 取得 Legacy Futures 報告：

| 商品 | 交易所 | 報告名稱 |
|------|--------|---------|
| 日圓 | CME | JAPANESE YEN - CHICAGO MERCANTILE EXCHANGE |
| 歐元 | CME | EURO FX - CHICAGO MERCANTILE EXCHANGE |
| 澳幣 | CME | AUSTRALIAN DOLLAR - CHICAGO MERCANTILE EXCHANGE |
| 原油 | NYMEX | CRUDE OIL, LIGHT 'SWEET' - NEW YORK MERCANTILE EXCHANGE |

產出 pkl 欄位包括：Noncommercial Long/Short、Open Interest、Net Position (Large Spec) 等。

### 4.2 TFF 報告擴充（E-mini S&P 500）

為 McElligott 儀表板新增 `fetch_cftc_tff_data()`，使用 `traders_in_financial_futures_fut` 報告類型：

| pkl 檔案 | 說明 |
|---------|------|
| `emini_spx_cme_net_position_asset_mgr.pkl` | Asset Manager 淨持倉 |
| `emini_spx_cme_net_position_lev_money.pkl` | Leveraged Fund 淨持倉 |
| `emini_spx_cme_net_pct_of_oi_asset_mgr.pkl` | AM 淨持倉佔 OI % |
| `emini_spx_cme_net_pct_of_oi_lev_money.pkl` | LF 淨持倉佔 OI % |

歷史深度：1,033 週（2006 年起）。

### 4.3 技術要點

- `COTReportCache` 支援 `report_type` 參數（`legacy_fut` / `traders_in_financial_futures_fut`）
- CFTC `report_name` 含連字號問題：`get_prefix_from_name()` 原用 `split('-')`，但 "E-MINI S&P 500" 自身含 `-`，改用 `split(' - ')` 修正
- TFF 報告名稱需 tuple 格式以處理名稱變更（`E-MINI S&P 500` vs `E-MINI S&P 500 STOCK INDEX`）
- 時間戳統一為 `08:00:00`（UTC+8）

## 五、儀表板使用現況

### 5.1 CFTC 頁面（`flask_highcharts/app/routes/CFTC.py`）

| 圖表 | 使用的 CME pkl |
|------|--------------|
| 日圓匯率與 CME 持倉 | `jpy_cme_open_interest` |
| 日圓投機性持倉多空 | `jpy_cme_noncommercial_long/short`, `jpy_cme_net_position_large_spec` |
| 歐元匯率與 CME 持倉 | `eur_cme_open_interest` |
| 歐元投機性持倉多空 | `eur_cme_noncommercial_long/short`, `eur_cme_net_position_large_spec` |
| 日圓空頭預測 VIX Peak | `jpy_cme_noncommercial_short` |

### 5.2 McElligott 相關性與部位（`McElligott相關性與部位.py`）

| 圖表 | 使用的 CME pkl |
|------|--------------|
| CFTC 多貨幣投機部位多空比率 | `jpy/eur/aud_cme_noncommercial_long/short` |
| 日圓 Carry Trade 壓力 | `jpy_cme_noncommercial_short`, `jpy_cme_net_position_large_spec` |
| E-mini SPX AM vs LF 淨部位 | `emini_spx_cme_net_position_asset_mgr/lev_money` |
| E-mini SPX 持倉 % of OI | `emini_spx_cme_net_pct_of_oi_asset_mgr/lev_money` |

### 5.3 資料更新流程

`get_all_series_data.py` 中註冊兩個 task：
- `("CFTC data", fetch_cftc_data)` — Legacy FX + 原油
- `("CFTC E-mini SPX TFF", fetch_cftc_tff_data)` — TFF E-mini SPX

## 六、總結與未來方向

### 已完成

| 項目 | 狀態 | 說明 |
|------|------|------|
| CME FTP 目錄盤點 | ✅ | 30 種 IRS 產品、daily_volume 歷史結構 |
| OIS 資料可行性測試 | ✅ | 缺 3M 期限，不適合替代 MacroMicro |
| 反爬蟲機制記錄 | ✅ | 只能用 Playwright + CME 帳號 |
| CFTC Legacy COT (FX + 原油) | ✅ | 日圓/歐元/澳幣/原油持倉 pkl |
| CFTC TFF (E-mini SPX) | ✅ | AM/LF 淨持倉 + %OI pkl |
| 儀表板整合 | ✅ | CFTC 頁面 + McElligott 相關性與部位 |

### 未開發 / 可探索

| 項目 | 優先級 | 障礙 |
|------|--------|------|
| daily_volume 日頻成交量+OI | 中 | 反爬蟲限制、3000+ xlsx 批量下載 |
| OIS 短期限 (3M) 替代來源 | 低 | CME 沒有，需找 Chatham Financial 或 BlueGamma |
| 更多 CFTC 商品 (GBP/CAD/NZD) | 低 | 技術上可行，但尚無儀表板需求 |

### 相關 Working Notes

- `WN-2026-04-01-CME-FTP-OIS-data.md` — OIS 資料取得測試
- `WN-2026-04-01-CME-FTP-資料總覽.md` — FTP 完整目錄盤點
- `WN-2026-04-02-McElligott-專有資料與替代方案.md` — CFTC 作為 HF positioning 替代
- `WN-2026-04-03-McElligott儀表板未完成工作盤點.md` — CFTC E-mini SPX 需求來源
- `WN-2026-04-03-McElligott波動率儀表板建置.md` — CFTC TFF 實作記錄
