# WN-2026-04-03 日圓日頻 OI 路由與回補指引

## 起因

CME FTP daily_volume 的日頻 OI/Volume 資料已可抓取（`get_cme_daily_volume.py`），pkl 也已產出（`cme_daily_6j_xcme_oi.pkl` 等），但尚未接上任何 Flask 路由。本次將日頻資料整合進日圓儀表板，並記錄歷史回補流程。

## 修改內容

### `flask_highcharts/app/routes/日圓.py`

在既有 6 張圖表之後，新增 3 張圖表：

| # | 標題 | Series IDs | 分析用途 |
|---|------|-----------|---------|
| Chart 7 | 日圓匯率 vs CME 日頻未平倉量 | `385`, `cme_daily_6j_xcme_oi` | 日頻解析度的 OI 變化，比 CFTC 週報更即時 |
| Chart 8 | 日圓匯率 vs CME 日頻成交量 | `385`, `cme_daily_6j_xcme_volume` | 量價配合判讀，Volume spike 識別事件轉折 |
| Chart 9 | CME 日頻 OI vs CFTC 投機淨持倉 | `cme_daily_6j_xcme_oi`, `jpy_cme_net_position_large_spec` | 日頻 OI 看「何時」、CFTC 看「誰」，交叉判讀 |

每張圖表均附帶詳細 summary，說明判讀邏輯與搭配方式。

### 未修改的檔案

- `get_cme_daily_volume.py` — 無變動，既有架構已支援
- `highcharts_utils.py` — `load_series_data()` 的 regex `(?:^|_){id}\.pkl$` 已確認能匹配 `cme_daily_6j_xcme_oi.pkl`

## 驗證結果

Flask 啟動後呼叫 `/api/chart/日圓` 確認 9 張圖表全部正常載入：

| Chart | 標題 | Series 1 | Series 2 |
|-------|------|----------|----------|
| 6 (新) | 日圓匯率 vs CME 日頻未平倉量 | fx-usd-jpy: 13469 pts | CME Japanese Yen Daily OI: 4 pts |
| 7 (新) | 日圓匯率 vs CME 日頻成交量 | fx-usd-jpy: 13469 pts | CME Japanese Yen Daily VOLUME: 4 pts |
| 8 (新) | CME 日頻 OI vs CFTC 投機淨持倉 | CME Japanese Yen Daily OI: 4 pts | Net Position, Large Spec: 6671 pts |

目前 CME 日頻資料僅 4 點（2026-03-27 ~ 2026-04-01），需回補歷史後圖表才有實質參考價值。

## 歷史回補指引

### 資料現況

- CME FTP 有 3000+ xlsx 檔案（2014-01-02 至今），每檔約 100KB，全部約 300MB
- 目前僅下載 4 檔（最近一次測試下載）
- `cme_daily_6j_xcme_oi.pkl` 僅 4 個數據點

### 回補步驟

```powershell
# 1. 啟動 Playwright 互動模式，登入 CME 帳號
python get_cme_daily_volume.py --backfill 365    # 先測一年

# 2. 確認下載正常後，逐步擴大
python get_cme_daily_volume.py --backfill 1500   # 約 4 年
python get_cme_daily_volume.py --backfill 4000   # 全部歷史（2014 起）

# 3. 全部下載完成後，重建 pkl
python get_cme_daily_volume.py --rebuild-pkl
```

### 注意事項

1. **反爬蟲限制**：需 Playwright `headless=False` + CME 帳號登入，無法全自動化
2. **斷點續傳**：已有 `list_local_dates()` 跳過已存在的 xlsx，session 過期重新登入後可繼續
3. **Rate limit**：每檔間隔 `DOWNLOAD_DELAY_SEC = 2.0` 秒，3000 檔約需 1.5~2 小時
4. **分批建議**：先 `--backfill 365` 測一年確認穩定，再逐步擴大範圍
5. **Session 過期**：CME cookies 會過期，過期時 LINE 會通知；手動跑 script 重新登入即可
6. **磁碟空間**：全部 xlsx 約 300MB，加上 pkl 影響不大

### 完成後效果

回補完成後，Chart 7/8/9 將有 2014 年至今的日頻數據，可清楚觀察：
- 2015 年人民幣貶值期間的 JPY OI 急降
- 2019 年 flash crash 的 OI 與 Volume 異常
- 2022 年 BOJ 干預期間的日頻 OI 變化
- 2024 年 carry trade unwind 事件的逐日展開
